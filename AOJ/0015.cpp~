#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    string s;
    int data[80];
    for(int i=0;i<n;i++){
        for(int j=0;j<80;j++){
            data[j] = 0;
        }
        cin >> s;
        for(int j=0;j<s.size();j++){
            data[s.size() - j - 1] = int(s[j] - '0');

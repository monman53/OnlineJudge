#include <iostream>
#include <vector>
using namespace std;

int main(){
  int a, b, c;
  while(1){
    cin >> a >> b >> c;
    if(a == 0 && b == 0 && c == 0) break;
    int n;
    cin >> n;
    


    for(int i=0;i<n;i++){

    }
  }
}

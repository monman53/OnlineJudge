#include <iostream>
#include <string>
using namespace std;

int main(){
    string s;
    cin >> s;
    int count=0;
    int size = s.size();
    for(int i=0;i<size/2;i++){
        count = count + ((s[i] == s[size - i - 1]) ? 0 : 1);
        
    return 0;
}

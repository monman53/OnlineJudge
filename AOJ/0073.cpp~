#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int x,h;
    while(1){
        cin >> x >> h;
        if(x == 0 && h == 0) break;
        cout << (x*x + x*sqrt(x*x + 4*h*h)) << '\n';
    }
    return 0;
} 

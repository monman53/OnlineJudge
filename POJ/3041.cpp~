#include <iostream>
#include <utility>
#include <cstdio>
#include <set>
using namespace std;

int main(){
    int n,k;
    bool data[500][500];
    int sumb[1000];
int 
    scanf("%d%d",&n,&k);    
    for(int i=0;i<n;i++){
        for(int j=0;j<n;j++){
            data[i][j] = false;
        }
        sumb[i*2] = sumb[i*2 + 1] = 0;
    }
    for(int i=0;i<k;i++){
        int a,b;
        scanf("%d%d",&a,&b);
        data[a-1][b-1] = true;
        sumb[(a-1)*2]++;
        sumb[(b-1)*2+1]++;
    }
    set<pair<int,int> > sum;


    return 0;
}

#include <iostream>
using namespace std;

int main(){
    int n,d;
    int x[1002],y[1002];
    bool pc[1002];
    cin >> n >> d;
    for(int i=0;i<n;i++){
        cin >> x[i+1] >> y[i+1];
        pc[i+1] = false;
    }
    while(1){
        char c;
        cin >> c;
        if(cin.eof()) break;
        if(c == 'O'){
            int p;
            cin >> p;
        }else{
            int f,t;
            cin >> f >> t;
        }
    }
    return 0;
}

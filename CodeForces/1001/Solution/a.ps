namespace Solution
{
    open Microsoft.Quantum.Canon;
    open Microsoft.Quantum.Primitive;

    operation Solve (q: Qubit, sign: Int) : () {
        if(sign == 1){
            H(q);
        }else{
            H(q);
            Z(q);
        }
    }
}